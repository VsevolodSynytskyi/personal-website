const initial = {
  opacity: 0,
  filter: "blur(10px)",
};
const animate = {
  opacity: 1,
  filter: "blur(0px)",
};

const blurRevealAnimation = {
  initial,
  animate,
};

export default blurRevealAnimation;
